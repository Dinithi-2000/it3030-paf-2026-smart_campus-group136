spring:
  application:
    name: smart-campus-backend

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://127.0.0.1:27017/smart-campus}
      database: smart-campus

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    org.springframework.security: INFO
