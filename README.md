# Smart Campus Operations Hub

This repository contains the IT3030 PAF 2026 group coursework project.

## Tech Stack
- Backend: Spring Boot REST API (Java 21)
- Frontend: React + Vite
- CI: GitHub Actions (build + test)

## Project Structure
- backend: Spring Boot API
- frontend: React web client

## Quick Start

### Backend
1. Open a terminal in `backend`
2. Run: `mvn spring-boot:run`
3. API starts on port 8080

### Frontend
1. Open a terminal in `frontend`
2. Run: `npm install`
3. Run: `npm run dev`
4. App starts on port 5173

## Assignment Notes
- Keep commit history active and member-specific.
- Document endpoint ownership and UI ownership in the final report.
- Add architecture diagrams, test evidence, and endpoint matrix before final submission.
