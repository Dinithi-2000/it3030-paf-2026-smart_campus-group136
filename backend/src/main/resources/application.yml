spring:
  application:
    name: smart-campus-backend

  data:
    mongodb:
      uri: mongodb+srv://smart_campus:campus123@smart-campus.0qzolar.mongodb.net/?appName=smart-campus
      database: smart-campus

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    org.springframework.security: INFO
